[pytest]
testpaths = tests
python_files = test_*.py
# Put the repo root on sys.path so `import game` works no matter how pytest is
# launched (bare `pytest` console script, CI, IDE — not just `python -m pytest`).
pythonpath = .
addopts = -q
